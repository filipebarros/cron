image: ruby:2.6

pipelines:
  default:
    - step:
        caches:
          - bundler
        script:
          - gem install bundler
          - bundle install
          - bundle exec rubocop
          - bundle exec rspec

definitions:
  caches:
    bundler: ./vendor
