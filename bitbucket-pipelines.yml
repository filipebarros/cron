image: ruby:2.6

pipelines:
  default:
    - step:
        caches:
          - bundler
        script:
          - gem install bundler
          - bundle install --path vendor/bundle
          - bundle exec rspec

definitions:
  caches:
    bundler: ./vendor
