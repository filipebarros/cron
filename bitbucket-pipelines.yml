image: ruby:2.6.3-alpine

pipelines:
  default:
    - step:
        caches:
          - bundler
        script:
          - bundle update --bundler
          - bundle install
          - bundle exec rubocop
          - bundle exec rspec

definitions:
  caches:
    bundler: ./vendor
